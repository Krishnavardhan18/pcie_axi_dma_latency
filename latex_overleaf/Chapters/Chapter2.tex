% Chapter 2

\chapter{Literature Review}

\label{Chapter2}

\lhead{Chapter 2. \emph{Literature Review}}

This chapter looks at the four main areas that this thesis is based on: how well the PCIe protocol works, designing DMA architectures using AXI, ways to reduce overhead from systems and networking research, and modeling latency in on-chip interconnects using RTL and queueing theory. Then, in section 2, we identify the specific problem that this thesis aims to solve.

%----------------------------------------------------------------------------------------

\section{PCIe Protocol and Performance Characterisation}
\label{sec:ch2_pcie}

The \ac{PCIe} Base Specification \cite{pcisig2017base} defines a layered architecture -- transaction, data link, and physical layers -- in which application data is carried inside \acp{TLP}. Every \ac{TLP} carries a fixed-size header (typically 12--16 bytes for memory requests), is subject to credit-based flow control between the transmitter and receiver, and its delivery is acknowledged at the data-link layer. Budruk~et~al.~\cite{budruk2004pcie} provide a detailed treatment of this layering and of the round-trip credit and acknowledgement mechanisms that contribute fixed, size-independent latency to every transaction, independent of the physical-layer transfer rate.

Empirical characterisation of this fixed-overhead behaviour is surprisingly thin on the ground. Neugebauer et al.~\cite{neugebauer2018pcie} measured PCIe performance for end-host networking on commodity servers and found that for small DMA transfers -- the case most relevant to packet-processing NICs -- throughput is set by the number of separate PCIe transactions issued, not by the total byte count; approaching link bandwidth required batching several descriptors or payloads into each transaction. Their measurements, however, were taken on production hardware with the NIC and host treated as a single black box: the paper does not attribute the observed overhead separately to the PCIe transaction layer versus the NIC's own descriptor processing, nor does it expose a way to vary that split experimentally. Gu et al.~\cite{gu2025cooptimization} target essentially the same symptom -- poor small-packet DMA bandwidth over PCIe -- from the opposite direction, modifying both the host driver (descriptor prefetching) and the endpoint hardware (a wider outstanding-transaction window, multicore dispatch) on a Xilinx VC709/XDMA board, and report a 123--136\% bandwidth gain and 15--24\% latency reduction under a randomised small-packet workload. That result is closer in spirit to this thesis than any other single paper found in this review, and it is adopted below as the primary point of comparison (\autoref{sec:ch2_gap}); but it, too, reports only aggregate GB/s and microsecond figures for a specific board and driver stack, rather than a packet-size-parameterised model of where the latency actually accumulates. Isolating that split is what motivated breaking the system down into separately-measurable modules in Chapter~4 of this thesis.

%----------------------------------------------------------------------------------------

\section{AXI-Based DMA Architectures}
\label{sec:ch2_axi_dma}

On the fabric side of a PCIe endpoint, the \ac{AMBA} \ac{AXI4} and \ac{AXI4-Stream} protocols \cite{arm2021axi, arm2010axistream} are the de facto standard for connecting a DMA engine to on-chip processing logic. AXI4 defines a memory-mapped, burst-oriented protocol with independent address and data channels, while AXI4-Stream strips away addressing entirely in favour of a simple, back-pressured (\texttt{tvalid}/\texttt{tready}) beat-by-beat handshake with sideband fields (\texttt{tlast}, \texttt{tkeep}, \texttt{tuser}, \texttt{tid}) that this thesis reuses directly to carry per-packet size and identity information through the pipeline (\autoref{sec:ch4_sideband}).

Commercial DMA engines, like the AMD-Xilinx AXI DMA and the DMA/bridge logic in the UltraScale+ PCIe endpoint block, use a descriptor-ring model to operate. Here's how it works: the host or an on-chip processor writes a descriptor with details like source address, destination address, and length to a ring buffer in memory. Then, it signals the engine using a doorbell register or interrupt. The engine has to fetch and parse this descriptor, which is a memory or PCIe transaction with its own latency, before it can start moving the payload. Kavianipour and B{\"o}hm's bus-master scatter/gather DMA interface for PCIe~\cite{kavianipour2012high} is a representative example of this class of engine in the research literature rather than a vendor datasheet: it reports throughput up to 1080~MB/s for large, page-aligned buffers, but explicitly notes that its own application used 12-byte buffers merged in software before the DMA call, because the mapping overhead of the scatter/gather mechanism is only worthwhile once a buffer is large -- an early, driver-level instance of exactly the amortisation problem this thesis addresses at the RTL level instead. The product guides for the AXI DMA and PCIe integrated-block cores similarly provide information on register maps, supported burst lengths, and peak throughput figures. However, they don't model descriptor-fetch latency as a function of transfer size, treating it as an implementation detail instead. In contrast, this thesis's DMA model makes the fixed descriptor-processing cost a clear, parameterized quantity, called SETUP CYCLES, so its contribution to small-packet latency can be isolated and measured rather than left implicit.

Two further strands of PCIe-FPGA interconnect research are worth placing alongside this one, mainly to mark out what this thesis does \emph{not} attempt. Shared virtual memory work such as FlexiMem~\cite{sonmez2024fleximem} targets a different bottleneck entirely: a configurable, multi-master address-translation interconnect that lets an FPGA accelerator fault in host pages on demand, rather than the fixed per-transfer cost of moving an already-addressed payload. Driver-level studies such as Bandara et al.'s comparison of VirtIO and vendor-specific device drivers for host-FPGA PCIe links~\cite{bandara2024performance} address portability and maintenance of the software stack sitting above the DMA engine, and find VirtIO drivers roughly match vendor drivers on latency; that is a software-architecture question about which driver issues the memory-mapped transactions, orthogonal to the RTL-level question of how many fixed-cost transactions a given amount of payload needs in the first place, which is what this thesis is concerned with.

%----------------------------------------------------------------------------------------

\section{Overhead Amortisation and Batching Techniques}
\label{sec:ch2_batching}

The idea behind the batching unit in this thesis is simple: combining many small events into fewer, larger ones can make them more efficient. This concept has been around for a while in systems and networking research, but it's usually applied in software or at a higher level, not directly in hardware like an RTL DMA datapath. For example, Mogul and Ramakrishnan found that when a network receives a lot of packets, handling each one with an individual interrupt can cause problems and slow things down. They showed that by polling and combining interrupts, so that multiple packets are handled at once, the system can recover and work more efficiently. This is because the fixed cost of handling each interrupt is spread out over many packets, making it more cost-effective. Later, network interface controllers started including hardware timers that could delay interrupts until a certain number of packets had arrived or a timeout had expired. This added a bit of latency to each packet, but it greatly reduced the overhead of handling each packet individually. By trading off a small amount of extra delay for a big reduction in overhead, these controllers could handle more packets and improve overall performance. This approach has been used in various forms, but the key idea is the same: by combining small events into larger ones, we can make systems more efficient and improve their performance. In the context of this thesis, the batching unit applies this principle to the RTL DMA datapath, aiming to reduce overhead and improve efficiency in a similar way.

These methods work at the point where the hardware and the operating system meet, combining signals that interrupt the system rather than the actual movement of data. Amortisation also appears at a much larger granularity in recent PCIe-attached accelerator work: Park et al.~\cite{park2024low} eliminate a redundant host-memory copy in PIM-to-PIM data movement over XDMA, reporting a 1.6--1.7$\times$ speedup for 16--128~MB transfers by routing peer-to-peer instead of via the CPU. That is a different overhead -- a whole extra data copy, at megabyte scale -- from the fixed per-transaction protocol cost this thesis targets at the tens-to-hundreds-of-bytes scale, but it is a useful reminder that "reduce the number of expensive round trips" is a recurring theme across the PCIe-DMA literature at every granularity. The concept of taking the same principle one step further down -- combining many small packets of data into one larger frame before they reach the parts of the system that add a fixed cost, and measuring how much this improves things directly in cycle-accurate simulation -- is what this project is about. It puts this idea into action with a part called the batching unit and checks how well it works with real numbers.

%----------------------------------------------------------------------------------------

\section{Latency Modeling of Interconnects}
\label{sec:ch2_latency_modeling}

Analytically, the behaviour this thesis is concerned with -- a service time made up of a fixed component plus a component proportional to the size of the job being served -- is a standard construction in queueing theory. Kleinrock's treatment of queueing systems \cite{kleinrock1975queueing} formalises service-time models of exactly this fixed-plus-proportional form and their consequences for waiting time and utilisation, and provides the conceptual basis for the closed-form PCIe and DMA delay expressions derived in \autoref{sec:ch3_latency_model}. However, classical queueing analysis models the service discipline abstractly; it does not, by itself, validate whether a specific RTL implementation of a specific protocol actually exhibits the assumed fixed-plus-proportional behaviour, or by how much a specific mitigation (such as batching) improves it for a specific packet-size distribution. Closing that gap between an analytical model and a cycle-accurate, simulatable hardware implementation -- built using standard SystemVerilog RTL practice \cite{ieee2017systemverilog} and simulated with an industrial simulator \cite{xilinx2022vivadosim} -- is the methodological contribution of this thesis.

%----------------------------------------------------------------------------------------

\section{Summary of Related Work}
\label{sec:ch2_summary}

The six studies reviewed above, together with the foundational protocol and modelling references, cover a wide spread of PCIe-DMA concerns -- bandwidth co-optimisation, memory sharing, driver portability, bulk-copy elimination, and RPC offload -- but only one addresses small-packet latency at the RTL level at all closely, and none provide a packet-size-parameterised, per-stage model. \autoref{tab:related_work} summarises where each sits relative to this thesis.

\begin{table}[htbp]
\centering
\begin{tabular}{@{}p{2.6cm}p{3.4cm}p{4.6cm}@{}}
\toprule
\textbf{Study} & \textbf{Focus} & \textbf{Relation to this thesis} \\
\midrule
Gu et al.~\cite{gu2025cooptimization} & Small-packet PCIe DMA bandwidth/latency (VC709, XDMA) & Closest prior work; adopted as the baseline for comparison (\autoref{Chapter7}); reports aggregate GB/s and $\mu$s, not a per-stage cycle model \\
Kavianipour \& B{\"o}hm~\cite{kavianipour2012high} & General bus-master scatter/gather DMA & Motivates buffer merging as an overhead-amortisation idea, applied here at RTL/frame granularity instead of in the driver \\
S{\"o}nmez et al.~\cite{sonmez2024fleximem} & Shared virtual memory for PCIe FPGAs & Different overhead (address translation), not fixed per-transfer cost \\
Bandara et al.~\cite{bandara2024performance} & Host-FPGA driver portability (VirtIO) & Software driver-stack question, orthogonal to RTL datapath latency \\
Park et al.~\cite{park2024low} & Bulk (MB-scale) PIM-to-PIM data movement & Removes a different overhead (redundant host copy) at a far larger transfer granularity \\
Zhang et al.~\cite{zhang2025rpcnic} & Datacenter RPC offload on PCIe SmartNICs & Illustrates PCIe fixed-cost concerns recurring one layer up, at the RPC datapath \\
Gupta et al.~\cite{gupta2024fpga} & FPGA-based low-latency trading & Motivational: a real system whose entire value proposition rests on the small-message latency studied here \\
\bottomrule
\end{tabular}
\caption{Related work surveyed in this chapter, and how each relates to the problem addressed by this thesis.}
\label{tab:related_work}
\end{table}

\section{Research Gap}
\label{sec:ch2_gap}

Synthesising the areas above, three gaps recur:

\begin{enumerate}
\item \textbf{No open, parameterised, per-stage latency model.} PCIe performance studies \cite{neugebauer2018pcie} characterise small-transfer inefficiency empirically on commodity hardware, and the closest hardware/software co-design effort \cite{gu2025cooptimization} improves on it substantially, but both report a single aggregate bandwidth or latency figure rather than a model that separately attributes latency to the PCIe transaction layer versus the DMA descriptor-processing stage. Vendor IP documentation \cite{xilinx2021axidma, xilinx2021pcieintblock} specifies the mechanisms that create this overhead (TLP framing, descriptor fetch) but does not quantify it as a function of packet size.
\item \textbf{No RTL-level batching for PCIe-AXI DMA transfers.} Coalescing is a well-established idea for network interrupts, and Gu et al.'s descriptor prefetching \cite{gu2025cooptimization} applies a related idea at the driver level, but applying frame-level coalescing directly inside an AXI-Stream DMA pipeline -- merging small packets into a larger frame before the PCIe/DMA overhead is paid, and measuring the effect at cycle granularity -- is not documented in the literature or vendor documentation surveyed here.
\item \textbf{No validated link between the analytical model and hardware measurement.} Fixed-plus-proportional service models are standard in queueing theory \cite{kleinrock1975queueing}, but whether a real PCIe-AXI DMA RTL pipeline actually behaves this way, and by how much a mitigation such as batching improves it, has not been demonstrated on a measurable, re-simulatable pipeline.
\end{enumerate}

This thesis addresses all three gaps together, using Gu et al.~\cite{gu2025cooptimization} as its primary point of comparison since it is the closest existing attempt at the same problem: Chapters~3 and~4 build a packet-size-parameterised model of a PCIe-AXI DMA pipeline, Chapter~5 implements the coalescing principle directly as a batching unit inside that pipeline, and Chapters~6 and~7 validate the model and quantify the batching improvement against hardware-measured latency, closing with a direct comparison back to Gu et al.'s results in \autoref{sec:ch7_comparison}.